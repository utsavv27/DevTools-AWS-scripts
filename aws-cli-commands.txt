configure aws account using iam role.

    aws configure

update the kube config of new cluster into local machine.

    aws eks --region <region> update-kubeconfig --name <cluster_name>


Here’s a list of important AWS CLI commands categorized by common AWS services:

### General AWS CLI Commands
- **Configure AWS CLI**:  

  aws configure
 
  Configures AWS CLI with access keys, region, and output format.

- **View Help**:  

  aws help
  aws <service> help

  Displays help for AWS CLI commands or specific services.

### Amazon S3 (Simple Storage Service)
- **List Buckets**:  

  aws s3 ls
  ```

- **Create a Bucket**:  
  ``` h
  aws s3 mb s3://my-bucket-name
  ```

- **Upload a File**:  
  ``` a
  aws s3 cp myfile.txt s3://my-bucket-name/
  ```

- **Download a File**:  
  ``` a
  aws s3 cp s3://my-bucket-name/myfile.txt ./
  ```

- **Sync Directories**:  
  ``` a
  aws s3 sync /local/dir s3://my-bucket-name/dir
  ```

### Amazon EC2 (Elastic Compute Cloud)
- **List Instances**:  
  ``` a
  aws ec2 describe-instances
  ```

- **Start an Instance**:  
  ``` a
  aws ec2 start-instances --instance-ids i-1234567890abcdef0
  ```

- **Stop an Instance**:  
  ``` a
  aws ec2 stop-instances --instance-ids i-1234567890abcdef0
  ```

- **Reboot an Instance**:  
  ``` a
  aws ec2 reboot-instances --instance-ids i-1234567890abcdef0
  ```

- **Terminate an Instance**:  
  ``` a
  aws ec2 terminate-instances --instance-ids i-1234567890abcdef0
  ```

### Amazon RDS (Relational Database Service)
- **List Databases**:  
  ``` a
  aws rds describe-db-instances
  ```

- **Create a Database**:  
  ``` a
  aws rds create-db-instance --db-instance-identifier mydbinstance --allocated-storage 20 --db-instance-class db.t2.micro --engine mysql --master-username admin --master-user-password password
  ```

- **Delete a Database**:  
  ``` a
  aws rds delete-db-instance --db-instance-identifier mydbinstance --skip-final-snapshot
  ```

### Amazon EKS (Elastic Kubernetes Service)
- **Update kubeconfig for EKS Cluster**:  
  ``` a
  aws eks --region <region> update-kubeconfig --name <cluster_name>
  ```

- **Describe EKS Cluster**:  
  ``` a
  aws eks describe-cluster --name <cluster_name>
  ```

### AWS IAM (Identity and Access Management)
- **List Users**:  
  ``` a
  aws iam list-users
  ```

- **Create a User**:  
  ``` a
  aws iam create-user --user-name myuser
  ```

- **Attach a Policy to a User**:  
  ``` a
  aws iam attach-user-policy --user-name myuser --policy-arn arn:aws:iam::aws:policy/AmazonS3FullAccess
  ```

### AWS Lambda
- **List Lambda Functions**:  
  ``` a
  aws lambda list-functions
  ```

- **Invoke a Lambda Function**:  
  ``` a
  aws lambda invoke --function-name my-function-name output.txt
  ```

- **Create a Lambda Function**:  
  ``` a
  aws lambda create-function --function-name my-function --runtime python3.8 --role arn:aws:iam::123456789012:role/service-role/my-role --handler lambda_function.lambda_handler --zip-file fileb://function.zip
  ```

### AWS CloudFormation
- **List Stacks**:  
  ``` a
  aws cloudformation list-stacks
  ```

- **Create/Update a Stack**:  
  ``` a
  aws cloudformation deploy --template-file template.yaml --stack-name my-stack
  ```

- **Delete a Stack**:  
  ``` a
  aws cloudformation delete-stack --stack-name my-stack
  ```

### AWS CloudWatch
- **List Metrics**:  
  ``` a
  aws cloudwatch list-metrics
  ```

- **Get Metric Statistics**:  
  ``` a
  aws cloudwatch get-metric-statistics --namespace AWS/EC2 --metric-name CPUUtilization --dimensions Name=InstanceId,Value=i-1234567890abcdef0 --start-time 2024-08-21T23:18:00Z --end-time 2024-08-22T23:18:00Z --period 3600 --statistics Average
  ```

- **Put a Custom Metric**:  
  ``` a
  aws cloudwatch put-metric-data --namespace "CustomNamespace" --metric-name MyMetric --value 1
  ```

### AWS ECS (Elastic Container Service)
- **List Clusters**:  
  ``` a
  aws ecs list-clusters
  ```

- **Describe a Cluster**:  
  ``` a
  aws ecs describe-clusters --clusters my-cluster
  ```

- **List Tasks in a Cluster**:  
  ``` a
  aws ecs list-tasks --cluster my-cluster
  ```

- **Run a Task**:  
  ``` a
  aws ecs run-task --cluster my-cluster --task-definition my-task
  ```

### AWS ECR (Elastic Container Registry)
- **List Repositories**:  
  ``` a
  aws ecr describe-repositories
  ```

- **Create a Repository**:  
  ``` a
  aws ecr create-repository --repository-name my-repo
  ```

- **Delete a Repository**:  
  ``` a
  aws ecr delete-repository --repository-name my-repo
  ```

- **Login to ECR**:  
  ``` a
  aws ecr get-login-password --region <region> | docker login --username AWS --password-stdin <aws_account_id>.dkr.ecr.<region>.amazonaws.com
  ```

These commands cover a wide range of AWS services and can be a solid foundation for managing your AWS infrastructure.
